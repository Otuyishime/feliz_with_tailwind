module.exports = {
    mode: 'jit',
    purge: [
        './src/**/*.fs',
    ],
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
}